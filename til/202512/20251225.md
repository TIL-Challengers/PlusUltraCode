### Github Action 을 이용한 Self-Hosted-Runner 이용하여 CICD 구축하기

먼저 Self-Hosted-Runner 를 쓰는 이유에 대해서 말해보겠다.

기존 Github Action을 이용하여 ssh를 통해 우리 컴퓨터로 접근하여 서버를 띄우는 형태였다. 
그러나 이 부분은 인바운드 규칙 특정 port를 열어나야 해서 보안상에 문제가 있다고 한다.

좋다 그러면 ec2가 스스로 github action에 있는 job을 감지한 뒤 작업을 시작하면 outbound만 열고 inbound규칙은 열지 않아도 되서 보안상에 좋다.

step by step으로 가보자.

1. 파일 압축 설치 
```
sudo apt update -y
sudo apt install -y curl tar gzip ca-certificates
```
주의점 : Outbound 443 반드시 열려 있어야 한다.

2. GitHub에서 러너 등록 토큰/명령 복사

Repository→Settings→Actions→Runner 들어가서 해당 os클릭 후 아래 명령어 그대로 따라 치면 된다.

만약 Runner의 label을 따로 설정하려고 한다면 아래 명령어에 label부분에 이름을 달면 된다.
<img width="657" height="395" alt="image" src="https://github.com/user-attachments/assets/2462afc7-2bfd-40f0-a2b0-b24b9a56bfb1" />


3. 서비스로” 띄우기 (서버 재부팅해도 자동 실행

```
sudo ./svc.sh install
sudo ./svc.sh start
sudo ./svc.sh status
```

여기까지 하면 GitHub Runners 목록에 Idle로 떠야한다.
<img width="727" height="283" alt="image" src="https://github.com/user-attachments/assets/e15ee24c-a2e5-423a-abcd-85b1effdfea1" />


5) 러너에 필요한 것 설치

```
sudo apt install -y docker.io
sudo usermod -aG docker $USER
newgrp docker
docker version

자바 jdk 도 버전에 맞게 설치하기
그리고 GitHub Actions에서 docker 쓰려면 runner 서비스 계정 권한이 중요함.
```


자주 실수하는 부분 조심 ex 권한설정
러너를 root로 등록/실행하지 말기
Docker permission
```
groups
sudo systemctl restart actions.runner.* 2>/dev/null ||true
sudo ./svc.sh restart

```

위의 flow을 잘 따라했고 repository에 idle 상태가 떠있다면 정상적으로 작동한거다.

그러면 띄우고 싶은 workflow를 수정해보자. 

현재 우리는 github Action에서 Ecr로 docker image를 업로드하고 self-hosted-runner를 이용하여 ECR에 접근 뒤 pull받아서 서버를 띄우는 형태로 작성할거다.

여기서 조심해야 될 부분은 ECR 권한설정이다. 이 부분은 수미랑 같이 해결하도록 해야 겠다.  현재는 EC2를 그대로 이용하던 거라 ECR 권한설정은 과거에 이미 되어 있을 것이다.

아래의 workflow를 보게 되면 

runs-on : self-hosted랑 bastion을 이용한 프록시 및  ssh접속 부분이 삭제된 것을 알 수있다.



```
name: Deploy To EC2

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: 🔧 Build & Push Docker Image to ECR
    # ✅ 기존처럼 GitHub-hosted runner 유지
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Github Repository 파일 불러오기
        uses: actions/checkout@v4

      - name: ☕ JDK 21버전 설치
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: 🧪 테스트 및 빌드하기 (현재 설정대로 test 제외)
        run: |
          chmod +x ./gradlew
          ./gradlew clean build -x test

      - name: 🔐 AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ap-northeast-2
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: 🔐 ECR 로그인
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # ✅ 기존 태그 정책 유지: IMAGE_NAME을 태그로 사용
      - name: 🐳 Docker 이미지 생성
        run: docker build -t ${{ secrets.IMAGE_NAME }} .

      - name: 🏷️ Docker 이미지에 Tag 붙이기
        run: |
          docker tag ${{ secrets.IMAGE_NAME }} \
            ${{ steps.login-ecr.outputs.registry }}/${{ secrets.REPOSITORY_NAME }}:${{ secrets.IMAGE_NAME }}

      - name: 🚀 ECR에 Docker 이미지 Push
        run: |
          docker push ${{ steps.login-ecr.outputs.registry }}/${{ secrets.REPOSITORY_NAME }}:${{ secrets.IMAGE_NAME }}

  deploy-to-ec2:
    name: 🚀 Deploy To EC2 (Self-hosted)
    # ✅ 배포만 self-hosted runner(EC2)에서 실행
    runs-on: self-hosted
    needs: build-and-push

    steps:
      - name: 🔐 AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ap-northeast-2
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: 🔐 ECR 로그인
        run: |
          aws ecr get-login-password --region ap-northeast-2 | \
            docker login --username AWS --password-stdin {password}

      - name: 🧹 Stop & Remove existing container
        run: |
          docker stop ${{ secrets.IMAGE_NAME }} || true
          docker rm ${{ secrets.IMAGE_NAME }} || true

      - name: 📥 Pull image from ECR
        run: |
          docker pull 869508798906.dkr.ecr.ap-northeast-2.amazonaws.com/${{ secrets.REPOSITORY_NAME }}:${{ secrets.IMAGE_NAME }}

      - name: 🚀 Run container
        run: |
          docker run -d --name ${{ secrets.IMAGE_NAME }} -P \
            --network ${{ secrets.DOCKER_NETWORK_NAME }} \
            -e S3_SECRET_KEY="${{ secrets.S3_SECRET_KEY }}" \
            -e EC2_HOST_APP="${{ secrets.EC2_HOST_APP }}" \
            -e EC2_HOST_APP_PRIVATE="${{ secrets.EC2_HOST_APP_PRIVATE }}" \
            -e EC2_HOST_INFRA="${{ secrets.EC2_HOST_INFRA }}" \
            -e EC2_HOST_INFRA_PRIVATE="${{ secrets.EC2_HOST_INFRA_PRIVATE }}" \
            -e S3_BUCKET="${{ secrets.S3_BUCKET }}" \
            -e S3_ACCESS_KEY="${{ secrets.S3_ACCESS_KEY }}" \
            -e DB_URL="${{ secrets.DB_URL }}" \
            -e DB_USERNAME="${{ secrets.DB_USERNAME }}" \
            -e DB_PASSWORD="${{ secrets.DB_PASSWORD }}" \
            -e JWT_SECRET="${{ secrets.JWT_SECRET }}" \
            -e EUREKA_DEFAULTZONE="${{ secrets.EUREKA_DEFAULTZONE }}" \
            869508798906.dkr.ecr.ap-northeast-2.amazonaws.com/${{ secrets.REPOSITORY_NAME }}:${{ secrets.IMAGE_NAME }}

```

<img width="713" height="591" alt="image" src="https://github.com/user-attachments/assets/25248a4f-752f-4038-b708-630166f20eb8" />

해당 self-hosted Runner 를 하면서 느낀점은 확실히 추상화가 잘 되어 있다는 느낌이다.
사용자는 내부 상세로직을 몰라도 interface형태로 쉽게 접근이 가능하다.
또한 기존 CICD workflow를 공부해 놓았기 때문에 살짝 응용만 하면 되었다. 

새로운 기술은 반드시 기존 기술의 응용이라고 생각하게 된 계기가 되었다.

실제 목적은 ec2가 아니라 학교 서버에 띄우는 형태라 ecr접근할수 있도록 학교 서버에 권한 설정 혹은 workflow에 ecr접근 허용할 만한 뭔가를 줘야 될거 같다.

그러나 문제가 있다. 현재는 하나의 repo의 runner를 만들어서 ec2에 등록했다.그러면 repo가 여러개일 경우 어떻게 될까?? 
3개의 repo가 있응면 3개의 runner을 등록해야 한다. 늘어날 수록 버거워진다.

찾아보니 organization으로 그룹 전체 runner를 등록할수 있다고 한다.

그럼 여기서 label능력을 부여해야 될거 같다.
label이란 다음과 같다.
ec2입장에서 job들이 있을건데 이게 내가 실행시켜도 되는지 말지 결정하는게 label이다
workflow에 runs-on : self-hosted 부분에 등록할 때 label형태로 등록을ㅇ 할 수있다.

현재 우리는 elastic 과 나머지 app.server를 분리했다.

organization 으로 하나의 runner를 이용해야 하기 때문에 label을 부여해야 한다.

예를들어 보자. 

elatsic-service 의 label 은 elastic이다. 
즉 해당 service workflow에 label에 elastic을 붙인다.

elastic-service에 있는 ec2에 있는 runner를 등록할 때 label을 elastic으로 해놓게 되면 Organization 안에 여러개의 jobs들이 있다고 해도 elastic label이 붙은 job만 실행하게 된다.
나머지 app-server도 같은 방식으로 실행하면 된다.

